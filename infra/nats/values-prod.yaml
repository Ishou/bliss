# Production overrides — Hetzner k3s (ADR-0009 §2).
image:
  # Pinned to the nats:2.10-alpine OCI image index digest resolved on
  # 2026-05-20. Refresh by re-running the registry digest-lookup against
  # the 2.10-alpine tag when bumping to a new minor.
  digest: "sha256:b83efabe3e7def1e0a4a31ec6e078999bb17c80363f881df35edc70fcb6bb927"
storage:
  size: 5Gi
  storageClass: "hcloud-volumes"
streamInit:
  image:
    # natsio/nats-box:0.19.5 OCI image index digest, resolved 2026-05-20.
    digest: "sha256:5d513bf0bb82d6af3fe8363097eb14d2e4ca3242db24140db88df4a4ba082442"

# Prometheus exporter sidecar — on in prod; digest pinned to multi-arch image index for natsio/prometheus-nats-exporter:0.15.0.
metricsExporter:
  enabled: true
  image:
    digest: "sha256:1f6787754b8b083f271adea4bb699b9c9e21279c89d903f1710f7647c604f145"

# DLQ stream — on in prod (scaling-readiness per ADR-0049).
dlq:
  enabled: true
