# Survey Rating Undo — Orchestration Log

Append-only log of decisions the orchestrator made during this rollout. For human review when convenient.

## Standing decisions

| Decision | Value | Rationale |
|---|---|---|
| Merge authority | Orchestrator merges on `LGTM` + green blocking CI (standard 3a) | Maintainer invoked `/orchestrate` in response to an explicit "hands-off cron-driven dispatch + auto-merge" offer (2026-05-30) |
| Polling cadence | 120 s (`*/2 * * * *` via CronCreate) | Matches dispatch-skill default; `claude-review` IN_PROGRESS = wait |
| Continuity | `CronCreate` (session-only in practice; `durable` flag ignored by runtime) | Recreate if the rollout outlives the session |
| Fix-cycle budget per phase | 3 | Dispatch-skill default |
| Phase order | Strictly sequential: bootstrap → 0.5 → 1 → 2a → 2b → 2c → 3; each branches off `main` after predecessor merges | 0.5 (ADR amendment) gates 1 (ADR-0001 §7); 2b needs 2a's tx helper; 2c needs 2b's repos; 3 needs 1 (types) + 2c (server behavior) |
| Cap override | Standing; orchestrator may invoke proactively to short-circuit 3c-loop-terminator. PR 2c pre-flagged cap-heavy | memory `feedback-standing-cap-override`, `feedback-cap-override-short-circuit` |
| Escalation trigger | 3 failed fix-cycles on any PR, OR identical non-cap finding across two §6a cycles | Stops the chain; logs an `ACTION` entry + `CronDelete` self |

## Pre-orchestration state

- Spec + plan authored on `fix/sondage-no-campaign-ux` (an unrelated campaign-UX branch); carried onto this clean `docs/survey-undo-orchestration` branch off `origin/main` so implementer agents branch from `main`.
- Campaign lifecycle (ADR-0059, `campaigns` table, 423-lock) is already on `main` — the undo rollout depends on it and branches cleanly.
- **Stashed:** `docs/superpowers/plans/2026-05-30-survey-campaign-lock-orchestration-log.md` wip (unrelated) — `git stash` entry "campaign-lock-log wip (unrelated)" on `fix/sondage-no-campaign-ux`. Restore after the rollout (or whenever returning to that branch).
- Bootstrap docs PR (this branch) carries: spec, plan, this procedure + log. Merges via standard 3a (memory `feedback-bootstrap-pr-uses-3a`). Phase 1 does not dispatch until the bootstrap PR is on `main`.

## Event log

- **2026-05-30 — Bootstrap PR #693 §6a cycles (all resolved by the auto-fixer loop):** Cycle 1 — ADR-0059 amendment bundled with implementation (Phase 2c) violated ADR-0001 §7; fixed `a0483eb2` by extracting standalone **Phase 0.5** (`docs/survey-undo-adr-amendment`, gates Phase 1). Cycle 2 — stale "ADR amendment" refs in Phase 4 intro / line 422; fixed `15519878`. Cycle 3 — multi-line KDoc in the Phase 2a `withTxConnection` snippet (pre-empts an implementer cycle); fixed `f563c60b`. Cycle 4 — **LGTM, no findings**.
- **2026-05-30 17:09 UTC — MERGED PR #693** (squash) → spec, plan, procedure, log now on `main`. Bootstrap branch auto-deleted. Phase 0.5 unblocked; dispatches next tick.
- **Note on log delivery:** `main` is branch-protected, so post-bootstrap log entries ride this orchestrator-owned branch (`docs/survey-undo-orch-log`) via PR rather than a direct push. One log PR for the rollout, appended each loggable tick.
- **2026-05-30 — Phase 0.5 · DISPATCHED implementer** (branch `docs/survey-undo-adr-amendment`, base `main`; docs-only ADR-0059 amendment). Bootstrap merged → dependency satisfied. ADR-context preflight empty for `docs/adr/**` (INDEX globs target source paths); implementer pointed at ADR-0059 + ADR-0001 §7. Flagged the `adr-index-coherence` may-reject-nonexistent-path caveat for the INDEX.md entry.
- **2026-05-30 17:17 UTC — Phase 0.5 · MERGED PR #695** (squash; `LGTM, no findings` + green blocking CI; CodeQL Analyze informational on a docs-only diff). ADR-0059 amendment (grace-window + export-at-close + `proposed_item_id` rationale) + `UndoActionUseCase.kt → ADR-0059` INDEX entry now on `main`. Implementer verified `registry-coherence` has no file-existence check, so the path-glob was safe to register now. **Phase 1 (schema) unblocked**; dispatches next tick.
- **2026-05-30 — Phase 1 · DISPATCHED implementer** (branch `feat/survey-undo-schema`, base `main`; schema-only `undoToken` + `POST /v1/actions/undo`). ADR preflight matched ADR-0003/0056/0059 (492 lines; agent told to run the helper + read in full). **Pre-empted a `regen-and-diff` failure:** the drift gate covers `frontend/src/infrastructure/api/survey/types.ts`, so the schema PR must regenerate + commit survey types (the plan's line-69 "regen waits for Phase 5" note is wrong for CI) — instructed the implementer to run `pnpm api:generate` and commit the generated types in this PR.
- **2026-05-30 17:33 UTC — Phase 1 · MERGED PR #696** (squash; claude-review `SUCCESS` + green blocking CI; CodeQL Analyze informational). Implementer's auto-fix loop resolved two §6a findings on `0be2d187` (added `undoToken` to `required` on both `RatingResponse`/`PairRatingResponse`, dropped the wrong `nullable` on the pair type per ADR-0003 §6). `regen-and-diff` green — survey `types.ts` regenerated in-PR as instructed. Schema (`undoToken` + `POST /v1/actions/undo`) now on `main`. **Phase 2a (tx boundary) unblocked**; dispatches next tick. (Local head branch retained — still bound to the implementer's worktree; remote auto-deleted.)
- **2026-05-30 — Phase 2a · DISPATCHED implementer** (branch `refactor/survey-tx-boundary`, base `main`; `TransactionManager` port + `PgTransactionManager` + `TxConnection` ambient element, refactor the 5 write repos to `withTxConnection` — NO behavior change). Plan tasks 2a.1–2a.4. ADR preflight matched ADR-0056/0059; agent told these are layering context only (no schema work this phase). Flagged the Testcontainers integration test (2a.4) may not run without local Docker — instructed the agent to report explicitly rather than silently skip.
- **2026-05-30 17:58 UTC — Phase 2a · MERGED PR #698** (squash; claude-review `SUCCESS` + green blocking CI; CodeQL informational). ~215 lines (84 main + 131 test), under the §4 cap. One §6a finding self-fixed on `9af74531` (concurrency-invariant KDoc on `TransactionManager.inTransaction` — block must not fan out to concurrent child coroutines over the non-thread-safe ambient `Connection`). Implementer notes: `withTxConnection` made `inline` to preserve non-local returns; assertk (not AssertJ) for the rollback assert; Testcontainers cases run in CI (`build (survey)` green), skipped locally for no-Docker. `TransactionManager` port + `PgTransactionManager` + `TxConnection` + 5 refactored write repos now on `main`. **Phase 2b (action log) unblocked**; dispatches next tick.
- **2026-05-30 — Phase 2b · DISPATCHED implementer** (branch `feat/survey-action-log`, base `main`; `V8__survey_actions` migration + `SurveyAction` domain + `TokenGenerator` port + `UndoTokenHash` util + `ActionLogRepository` port + `PgActionLogRepository` adapter + 5 reversal repo methods). Plan tasks 2b.1–2b.6. Ports + adapters + migration + domain ONLY — no use-case/route/DI wiring (that is 2c). ADR preflight ADR-0056/0059; flagged the 0059 undo amendment governs the `survey_actions` DDL (`proposed_item_id` column is load-bearing for 2c). Constrained token hash to JDK/Kotlin stdlib (no new crypto dep). Flagged Testcontainers-in-CI note again.
- **2026-05-30 18:43 UTC — Phase 2b · MERGED PR #703** (squash; claude-review `SUCCESS` + green blocking CI; CodeQL Analyze informational). Branched `feat/survey-action-log-2` (a stale local `feat/survey-action-log` forced the `-2` suffix). +653/−1 over 32 files — over the §4 cap, but bulk is mechanical fake/test interface-conformance updates; new production logic is small. Two §6a findings self-fixed: (1) cap-citation — agent added a Size note citing the ADR-0001 §4 2026-05-25 coherent-layer override (domain+ports+adapter land together to avoid a broken intermediate state); (2) multi-line migration comment trimmed to one ADR-0059 line on `db158dcd`. Migration renumbered **V8→V9** (`V9__survey_actions.sql`; V8 taken by the parallel training-weight workstream #699). `UndoTokenHash` uses JDK `MessageDigest` sha256 (no new dep); `SurveyAction` identity-on-`id`; 5 ports + 5 PG adapters each gained one reversal method. Scope held (no use-case/route/DI). `survey_actions` action-log layer now on `main`. **Phase 2c (undo use-case + wiring) unblocked**; dispatches next tick (pre-flagged cap-heavy — implementer cites §4 override in PR body from first push).
- **2026-05-30 — Phase 2c · DISPATCHED implementer** (branch `feat/survey-undo-usecase`, base `main`; `UndoActionUseCase` + transactional submit-wiring that mints the undo token & writes the reversal recipe + `PgRatingRepository` export settling filter + `AnonymizeUserRatingsUseCase` action scrub + `POST /v1/actions/undo` route + DI wiring). Plan Phase 4, tasks 2c.1–2c.6 (2c.7 done in Phase 0.5). ADR preflight matched ADR-0034/0048 (CORS — new route; held to the `allowHeaders { true }` predicate, not a literal `*`/allowlist), ADR-0056, ADR-0059 (undo amendment binds grace-window 410 + 404 semantics, export-at-close, `proposed_item_id` recipe). **Pre-flagged cap-heavy: implementer instructed to cite the ADR-0001 §4 2026-05-25 coherent-layer override in the PR body from the first push** (use-case + token-minting submit-wiring + route + DI land together; a partial split leaves a token minted with no redeeming route). Flagged Testcontainers-in-CI note.
- **2026-05-30 ~19:40 UTC — Phase 2c · ACTION / ESCALATION (multi-agent collision; held for maintainer).** First implementer `ae89a63` produced no Phase 2c work (its worktree HEAD sits on the merged 2b commit `77639fb0`); re-dispatched `a6c744a`, which is now confused — its worktree is on the parallel training-weight commit `c81ad294` (#705) with an unresolved `UU survey/api/.../Main.kt` merge conflict, i.e. wrong base. Separately, a **complete Phase 2c implementation** was found orphaned in the **main worktree** as a detached-HEAD commit `7bde95f2` (`feat(survey): undo use case + action-log integration (Phase 2c)`, +1054/−151 over 30 files, already rebased onto `origin/main`; file list matches the plan exactly — `UndoActionUseCase`, `UndoActionRoute`, submit-usecase token minting, `PgRatingRepository` export-settling, `AnonymizeUserRatingsUseCase` scrub, DI wiring, full tests). **Provenance unclear** — the main worktree should never have acquired a maintainer-attributed implementation commit (reflog: checkout `feat/survey-action-log` → commit → rebase onto `origin/main` → 2× amend). Preserved non-destructively as branch **`feat/survey-undo-usecase-recovered`**; junk staged on top (a half-edit to `SubmitRatingUseCaseTest.kt` + an unrelated grid test file) stashed as `main-worktree-pollution-during-2c-collision`. Main worktree restored clean to `docs/survey-undo-orch-log`. **No PR pushed** — shipping a commit of murky provenance (and unverified build, given the uncommitted half-edit on top) is outside the normal dispatch→review→merge grant; awaiting maintainer decision: (a) push `feat/survey-undo-usecase-recovered` as the Phase 2c PR and let CI + claude-review validate, or (b) discard it and re-run a clean implementer off `origin/main`.
- **2026-05-30 ~19:55 UTC — Phase 2c · RESOLVED → PR #707 (maintainer chose "verify + push recovered").** Re-dispatch `a6c744a` recovered itself, took the same Phase 2c work, fixed CI (notably `survey-export-csv-byteequal`), and pushed it as **PR #707** (`feat/survey-undo-usecase-2c` @ `a318e7f0`, +1052/−107 over 32 files — cap-heavy, §4 coherent-layer override applies). #707 supersedes the local safety branch `feat/survey-undo-usecase-recovered`. Both tangled agents wound down: `a6c744a` completed; `ae89a63` stopped via TaskStop (it had also built clean locally but never pushed). #707 status: MERGEABLE, all early gates green (`survey-export-csv-byteequal` pass); pending `build (survey)` (Testcontainers), `build`, `claude-review`. **Decision-tree verdict: WAIT** (3b/3e — CI + claude-review in progress); merge next tick on claude-review SUCCESS + green blocking CI, citing the §4 override for the cap.
- **2026-05-30 ~20:10 UTC — Phase 2c · FIXER dispatched (3c; fix-pass 1/3).** PR #707 `build` job failed on `:survey:infrastructure:test` → `PgRatingRepositoryTest > aggregateForExport excludes open and within-grace campaigns()` threw `org.postgresql.util.PSQLException` (malformed export-settling SQL, not a flake). Dispatched fixer `a25453dc` (worktree, bg) on `feat/survey-undo-usecase-2c`: diagnose the PSQLException in `PgRatingRepository.aggregateForExport`, fix the ADR-0059 export-at-close query (open + within-8s-grace EXCLUDED, past-grace INCLUDED), validate `:survey:infrastructure:test` (Testcontainers/Docker), commit -s + push. ADR preflight ADR-0056/0059 saved to `/tmp/adr_2c_fixer.txt`; instructed to read migration SQL for exact column shape rather than guess. Budget 3 passes.
- **2026-05-30 ~20:25 UTC — Phase 2c · FIXER pass 1 RESOLVED the SQL fail (commit `900b348a`).** Fixer `a25453dc` reported the `PSQLException` was a *test-fixture* bug, not a SQL bug: the test built three `survey_items` from `sampleItem()` changing only `id`, leaving identical `(mot, definition)` → the V5 partial unique index `survey_items_content_uniq` rejected the 2nd insert before `aggregateForExport` ran. Fix gave the open/grace items distinct `mot`/`definition`; the export-settling query is correct per ADR-0059 (`JOIN campaigns … WHERE closed_at IS NOT NULL AND closed_at < settledBefore`, settledBefore = now−8s). `build` (runs `:survey:infrastructure:test`) green on `900b348a`; Docker-unavailable locally so validated via CI.
- **2026-05-30 ~20:30 UTC — Phase 2c · FIXER dispatched (3c; review findings).** claude-review (COMMENTED) left 2 open findings on `900b348a` (CLOSE_GRACE extraction already self-resolved): **F1 (substantive) TOCTOU race** — the `undoneAt != null` guard in `UndoActionUseCase` sits *outside* the tx, so concurrent same-token requests double-redeem; fix = make `markUndone` a conditional `UPDATE … WHERE undone_at IS NULL` returning Boolean (port + PG adapter + in-memory fake) and move the gate inside the tx (2nd caller → `NotFound`). **F2 (style)** multi-line comment block in `byteequal/seed.sql:20-22` → collapse to one line (comment-only, keeps byte-equal green). Dispatched fixer `aba6c064` (worktree, bg) with both fixes specified verbatim + test for the double-redeem case. Budget 3 passes. All blocking CI otherwise green; only CodeQL `Analyze` (informational) still pending.
- **2026-05-30 ~20:45 UTC — Phase 2c · FIXER pass RESOLVED both review findings (pushed `0a8df04c`, `f82ec7da`).** Fixer `aba6c064`: **F1 (TOCTOU)** — `ActionLogRepository.markUndone(id, at): Boolean` adapter runs `UPDATE survey_actions SET undone_at=? WHERE action_id=? AND undone_at IS NULL` returning `executeUpdate() > 0`; in-memory fake flips + returns true only when currently null (UserDeletedConsumerTest fake returns false); gate moved INSIDE the tx (`val claimed = markUndone(); if (!claimed) NotFound else { reverse(); Undone }`), pre-tx `findByTokenHash ?: NotFound` kept as fast-path; 410/404 semantics unchanged. **F2** — 3-line `seed.sql` comment collapsed to one (data rows untouched → byteequal unaffected). Local: `:survey:application:test` + spotless green; infra Testcontainers Docker-unavailable, relying on CI. PR comment posted as the fixer mapping findings→SHAs. CI re-running on `f82ec7da`. **WAIT for CI + claude-review re-pass; merge next on clean LGTM.**
- **2026-05-30 20:25 UTC — Phase 2c · MERGED PR #707** (squash → merge commit `ab32aa43`; `feat/survey-undo-usecase-2c` @ `f82ec7da`). claude-review **"LGTM, no findings"** (all three findings across two cycles explicitly Resolved: CLOSE_GRACE extraction, TOCTOU conditional-`markUndone`, seed.sql comment) + green blocking CI; CodeQL `Analyze` informational. +1052/−107 over 32 files — cap-heavy, ADR-0001 §4 coherent-layer override cited in PR body (use-case + token-minting submit-wiring + route + DI land together). Provenance recap: the impl came from the orphan main-worktree commit `7bde95f2`, recovered + pushed by re-dispatch `a6c744a`, then two fix cycles (SQL fixture dup-`(mot,definition)` vs V5 unique index by `a25453dc`; review-findings by `aba6c064`). **Undo use-case + submit-wiring + `POST /v1/actions/undo` route + export-at-close settling + anonymize scrub + DI now on `main`. Phase 2c complete → Phase 3 (frontend) unblocked** (needs Phase 1 types + Phase 2c server; both merged); dispatches next tick.
- **2026-05-30 ~20:55 UTC — Phase 3 (frontend) · DISPATCHED implementer `a31818fb` (worktree, bg).** Final phase. Branch `feat/survey-frontend-undo` off `origin/main`; base `main`. Scope = plan "Phase 5 — Frontend PR" Tasks 3.1–3.8: regenerate `survey/types.ts` via `pnpm api:generate` (schema already on main → `regen-and-diff` will pass); extend `application/survey/types.ts` (`undoToken` on `RatingResult`, new `PairRatingResult`, `undoAction(token)` on `SurveyClient`, `remove(itemId)` on anon store); HTTP adapter `client.ts` (`undoAction` + `UndoExpiredError`/`UndoUnavailableError` for 410/404, pair-token passthrough); `localStorageSurveyAnon.ts` `remove`; new `UndoBar.tsx` (44px target + `_focusVisible` outline per ADR-0050); wire on-card `Annuler` into both `sondage.lazy.tsx` + `sondage.pairs.lazy.tsx` (stash item/pair, re-present on undo, analytics events); vitest/MSW tests. ADR preflight inlined: ADR-0003/0033/0050/0054/0059. Told to `/frontend`, follow the uncontrolled-input + tests-under-`frontend/tests/` + generated-`types.ts`-never-hand-edited conventions, and run the full `typecheck|lint|test|build`+`api:check` gate before push. Cap: generated `types.ts` excluded from §4 count; phase expected modest. Self-driving auto-fix loop, budget 3. **WAIT for PR + CI next tick.**
