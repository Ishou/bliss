apiVersion: v1
kind: Service
metadata:
  name: {{ include "bliss-identity-api.fullname" . }}
  labels:
    {{- include "bliss-identity-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bliss-identity-api.selectorLabels" . | nindent 4 }}
