// Frontend UI-domain layer. Per ADR-0002 §7, this layer holds pure UI
// domain types (e.g. GridView, CellState) with zero dependencies on
// application, infrastructure, or vendor SDKs. No types are exported yet
// because no behavior has shipped — types arrive with the first feature.
export {};
