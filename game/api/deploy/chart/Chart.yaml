apiVersion: v2
name: bliss-game-api
description: "Bliss multiplayer game backend (Ktor WebSocket)"
type: application
# Chart version. Bump when chart shape changes.
version: 0.1.0
# App version. Overridden per-deploy by the CD workflow (ADR-0009 §4).
appVersion: "0.1.0"
# Cluster operators (cert-manager, ingress-nginx, external-dns) are
# cluster-installed once via the bring-up workstream (ADR-0009 §3), not
# pulled in as subchart dependencies here. Game/ has no DB in v1
# (ADR-0018 §3 — lobbies live in-memory in :game:infrastructure), so
# unlike grid/ this chart carries no CNPG `Cluster` either.
dependencies: []
