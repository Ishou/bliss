apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordsparrow-api.fullname" . }}
  labels:
    {{- include "wordsparrow-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wordsparrow-api.selectorLabels" . | nindent 4 }}
