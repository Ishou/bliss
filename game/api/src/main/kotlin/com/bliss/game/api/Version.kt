package com.bliss.game.api

/**
 * Application version surfaced on `/v1/health`. Mirrors the Gradle
 * `version` declared in `game/api/build.gradle.kts`. Bump in lockstep
 * with the build script on each release.
 */
internal const val APP_VERSION: String = "0.1.0"
