<?xml version="1.0" encoding="UTF-8"?>
<!--
  Structured JSON logs (MANIFESTO Observability + ADR-0007 §7). STDOUT only;
  Fly captures the stream. correlation_id is a placeholder until a request
  hook populates MDC.
-->
<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service_name":"grid-api","bounded_context":"grid"}</customFields>
            <includeMdcKeyName>correlation_id</includeMdcKeyName>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
                <levelValue>[ignore]</levelValue>
                <version>[ignore]</version>
            </fieldNames>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
