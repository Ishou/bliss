apiVersion: v1
kind: Service
metadata:
  name: {{ include "bliss-game-api.fullname" . }}
  labels:
    {{- include "bliss-game-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bliss-game-api.selectorLabels" . | nindent 4 }}
