<?xml version="1.0" encoding="UTF-8"?>
<!--
  Structured JSON logs (MANIFESTO Observability + ADR-0007 Section 7). STDOUT only.
  The CronJob runtime captures the stream; service_name distinguishes worker
  events from grid-api events in SigNoz log queries.
-->
<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service_name":"grid-worker","bounded_context":"grid"}</customFields>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
                <levelValue>[ignore]</levelValue>
                <version>[ignore]</version>
            </fieldNames>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
