package com.bliss.game.api.auth

/** Cookie key constants shared between game-api routes. */
object CookieNames {
    /** Mirrors `identity/api/.../auth/SessionCookies.NAME` post-PR #526. */
    const val SESSION = "__Secure-ws_session"
}
